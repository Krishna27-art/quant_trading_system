global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'quant_terminal'
    metrics_path: '/metrics'
    static_configs:
      # If FastAPI is running on host machine port 8000
      - targets: ['host.docker.internal:8000']
